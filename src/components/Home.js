import React from 'react';
import styled from 'styled-components/macro';

const Home = () => {
  return (
    <Wrapper>
      
    </Wrapper>
  );
};

const Wrapper = styled.div `

`

export default Home;